{% extends 'base.html.twig' %}

{% block title %}Modification de chantier{% endblock %}

{% block body %}
    <br>
    <h1 class="text-center">Modification de chantier</h1>
    <br>



<table class="table table-bordered align-content-center mx-auto" style="width: 90%">
    <thead>
    <tr class="thead-dark ">
        <th scope="col">
            {{ include('chantier/_form.html.twig', {'button_label': 'Enregistrer'}) }}

            <button type="button" class="btn btn-outline-primary"><a href="{{ path('chantier_index') }}">Retour à la liste</a></button>

            {{ include('chantier/_delete_form.html.twig') }}
        </th>
    </tr>

    </thead>
</table>


{% endblock %}
