<form method="post" action="{{ path('phase_delete', {'id': phase.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phase.id) }}">
    <button class="btn">Delete</button>
</form>
