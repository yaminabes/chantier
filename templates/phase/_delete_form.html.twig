<form method="post" action="{{ path('phase_delete', {'id': phase.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phase.id) }}">
    <button class="btn btn-outline-danger mx-auto">Supprimer</button>
</form>
