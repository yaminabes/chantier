<form method="post" action="{{ path('unite_delete', {'id': unite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unite.id) }}">
    <button class="btn">Delete</button>
</form>
