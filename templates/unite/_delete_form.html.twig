<form method="post" action="{{ path('unite_delete', {'id': unite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unite.id) }}">
    <button class="btn btn-outline-success mx-auto">Supprimer</button>
</form>
