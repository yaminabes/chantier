<form method="post" action="{{ path('propose_delete', {'id': propose.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ propose.id) }}">
    <button class="btn">Delete</button>
</form>
