{% extends 'base.html.twig' %}

{% block title %}MaitreOuvrage index{% endblock %}

{% block body %}
    <h1>MaitreOuvrage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NumeroMatricule</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for maitre_ouvrage in maitre_ouvrages %}
            <tr>
                <td>{{ maitre_ouvrage.id }}</td>
                <td>{{ maitre_ouvrage.numeroMatricule }}</td>
                <td>
                    <a href="{{ path('maitre_ouvrage_show', {'id': maitre_ouvrage.id}) }}">show</a>
                    <a href="{{ path('maitre_ouvrage_edit', {'id': maitre_ouvrage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('maitre_ouvrage_new') }}">Create new</a>
{% endblock %}
