<form method="post" action="{{ path('materiaux_necessaires_delete', {'id': materiaux_necessaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiaux_necessaire.id) }}">
    <button class="btn">Delete</button>
</form>
