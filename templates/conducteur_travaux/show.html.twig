{% extends 'base.html.twig' %}


{% block body %}

    <br>
    <h1 class="text-center">Conducteur de Travaux {{ conducteur_travaux.id }}</h1>
    <br>

    <table class="table table-bordered align-content-center mx-auto" style="width: 90%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conducteur_travaux.id }}</td>
            </tr>
            <tr>
                <th>Numero de Matricule</th>
                <td>{{ conducteur_travaux.numeroMatricule }}</td>
            </tr>
            </tbody>
    </table>

    <div style="margin-left: 100px">

        <button type="button" class="btn btn-outline-primary"><a href="{{ path('conducteur_travaux_index') }}">Retour à la liste</a></button>

        <button type="button" class="btn btn-outline-warning"><a href="{{ path('conducteur_travaux_edit', {'id': conducteur_travaux.id}) }}">Modifier</a></button>

        <br><br>
        {{ include('conducteur_travaux/_delete_form.html.twig') }}
    </div>

{% endblock %}
