{% extends 'base.html.twig' %}

{% block title %}ConducteurTravaux{% endblock %}

{% block body %}
    <h1>ConducteurTravaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conducteur_travaux.id }}</td>
            </tr>
            <tr>
                <th>NumeroMatricule</th>
                <td>{{ conducteur_travaux.numeroMatricule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conducteur_travaux_index') }}">back to list</a>

    <a href="{{ path('conducteur_travaux_edit', {'id': conducteur_travaux.id}) }}">edit</a>

    {{ include('conducteur_travaux/_delete_form.html.twig') }}
{% endblock %}
