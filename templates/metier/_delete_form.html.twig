<form method="post" action="{{ path('metier_delete', {'id': metier.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metier.id) }}">
    <button class="btn btn-outline-danger mx-auto">Supprimer</button>
</form>
