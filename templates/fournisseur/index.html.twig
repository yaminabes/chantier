{% extends 'base.html.twig' %}


{% block body %}

    <br>
    <h1 class="text-center">Fournisseur </h1>
    <br>


    <table class="table table-bordered align-content-center mx-auto" style="width: 90%">
        <thead class="thead-dark ">
        <tr>
            <th scope="col">Id</th>
                <th>Nom_fournisseur</th>
                <th>Siret</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.id }}</td>
                <td>{{ fournisseur.nomFournisseur }}</td>
                <td>{{ fournisseur.siret }}</td>
                <td>
                    <button type="button" class="btn btn-outline-primary"><a href="{{ path('fournisseur_show', {'id': fournisseur.id}) }}">Voir</a></button>
                    <button type="button" class="btn btn-outline-warning"><a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}">Modifier</a></button>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucune donnée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div style="margin-left: 100px">
        <button type="button" class="btn btn-outline-success mx-auto"><a href="{{ path('fournisseur_new') }}">Ajouter</a></button>
    </div>

{% endblock %}
