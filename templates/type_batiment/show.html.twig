{% extends 'base.html.twig' %}

{% block title %}TypeBatiment{% endblock %}

{% block body %}
    <h1>TypeBatiment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_batiment.id }}</td>
            </tr>
            <tr>
                <th>NomType</th>
                <td>{{ type_batiment.nomType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_batiment_index') }}">back to list</a>

    <a href="{{ path('type_batiment_edit', {'id': type_batiment.id}) }}">edit</a>

    {{ include('type_batiment/_delete_form.html.twig') }}
{% endblock %}
