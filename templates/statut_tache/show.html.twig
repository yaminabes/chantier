{% extends 'base.html.twig' %}


{% block body %}
    <br>
    <h1 class="text-center">Statut tache : {{ statut_tache.nomStatut }}</h1>
    <br>

    <table class="table table-bordered align-content-center mx-auto" style="width: 90%">
        <tbody>
        <tr>
            <th>Id</th>
                <td>{{ statut_tache.id }}</td>
            </tr>
            <tr>
                <th>Nom_statut</th>
                <td>{{ statut_tache.nomStatut }}</td>
            </tr>
        </tbody>
    </table>

    <div style="margin-left: 100px">

        <button type="button" class="btn btn-outline-primary"><a href="{{ path('statut_tache_index') }}">Retour à la liste</a></button>

        <button type="button" class="btn btn-outline-warning"><a href="{{ path('statut_tache_edit', {'id': statut_tache.id}) }}">Modifier</a></button>

        <br><br>
        {{ include('statut_tache/_delete_form.html.twig') }}
    </div>
{% endblock %}
