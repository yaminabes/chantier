{% extends 'base.html.twig' %}

{% block title %}StockTacheUtilise{% endblock %}

{% block body %}
    <h1>StockTacheUtilise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_tache_utilise.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_tache_utilise_index') }}">back to list</a>

    <a href="{{ path('stock_tache_utilise_edit', {'id': stock_tache_utilise.id}) }}">edit</a>

    {{ include('stock_tache_utilise/_delete_form.html.twig') }}
{% endblock %}
