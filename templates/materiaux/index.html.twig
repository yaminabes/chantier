{% extends 'base.html.twig' %}

{% block title %}Materiaux{% endblock %}

{% block body %}

    <h1>Materiaux index</h1>
        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Nom_materiaux</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for materiaux in materiauxes %}
                <tr>
                    <td>{{ materiaux.id }}</td>
                    <td>{{ materiaux.nomMateriaux }}</td>
                    <td>
                        <a href="{{ path('materiaux_show', {'id': materiaux.id}) }}">show</a>
                        <a href="{{ path('materiaux_edit', {'id': materiaux.id}) }}">edit</a>
                    </td>
                </tr>


            {% else %}
                <tr>
                    <td colspan="5">Aucune donnée</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
<a class="btn btn-primary" href="{{ path('materiaux_new') }}">Ajouter</a>





{% endblock %}
